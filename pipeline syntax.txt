pipeline {
    agent any
    stages {
        stage ('pull code') {
            steps {
                sh 'ls'
            }
        }
        
	stage ('build code') {
            steps {
                sh 'echo "build code successfully"'
            }
        }
        stage ('test code') {
            steps {
                sh 'echo "test code successfully"'
            }
        }
        stage ('deploy code') {
            steps {
                sh 'echo "deploy code successfully"'
            }
        }
    }
}
                
                
